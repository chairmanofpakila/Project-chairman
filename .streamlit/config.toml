[theme]
# Primary accent color (buttons, sliders, selection)
primaryColor = "#CD2500"

# Backgrounds and text tuned for readability and contrast
backgroundColor = "#FFFFFF"
secondaryBackgroundColor = "#FFF3F0"
textColor = "#0E1117"
font = "sans serif"

